channels:
  - defaults
  - conda-forge

dependencies:
  - pip
  - ipython
  - nbsphinx
  - numpydoc
  - sphinx>=1.3
  - astropy  # need this here, because we base classes off it (and docs need that).
  - pyyaml
  - pip:
    - sphinx-rtd-theme
